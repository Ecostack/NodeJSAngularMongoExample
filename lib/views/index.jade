extends layout

block content
  div(ng-controller="MainController")
    h1= title
    p Welcome to #{title}
    button(ng-click="buttons.generateMeetings()") Generate meetings
    p
      a(href='/partials/upcoming') Upcoming meetings
      a(href='/partials/genmeetings') Generate meetings
      a(href='/partials/genmeetings') Generate meetings

